    pub enum Status {
        Connected,
        Interrupted,
    }

    pub fn connect_to_database() -> Status {
    // connect to Data Base
        Status::Connected
    }

    pub fn get_user() {
    //  fetch the user from Database3 and return
    }
